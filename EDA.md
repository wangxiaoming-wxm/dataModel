# 数据探索与建模决策

## 数据边界

| 项目 | 结果 |
|---|---:|
| 训练集 | 21,328 行，43 个可用特征 |
| 测试集 | 10,000 行 |
| 正例率 | 10.0009% |
| 训练/测试 ID 重叠 | 0 |
| 训练/测试完全相同特征行 | 0 |
| 重复 ID | 0 |
| 训练测试对抗验证 AUC | 0.491 |

对抗验证使用合并后的训练/测试来源作为目标。AUC 接近 0.5，说明两部分在现有
字段上的分布一致，没有证据支持特殊的分布漂移处理。

## 泄漏检查

- `id` 是唯一的六位十六进制标识；其训练集单变量 AUC 为 0.504，且与测试集
  无重叠。它被硬性排除，避免模型学习无业务意义的随机顺序。
- 测试集不含 `label`；提交样例的 `label` 全为占位值 0，不作为特征或训练数据。
- 未发现训练/测试完全重复特征行，不能也不会通过近邻复制标签。
- `cc`、`condition`、`max_g` 等高基数字段保留数值含义，不进行全量数据目标编码。
- 所有新增特征只由当前行原始字段计算，不访问标签和折外统计量。

## 信号结构

单变量探索中，`days` 的 AUC 为 0.5935，是最强的独立信号；其风险整体随暴露
增加，但最高区间出现回落，因此不强加全局单调约束。`condition` 的线性方向
AUC 为 0.5254，但分箱后呈明显非线性：最低两个分位区间的索赔率约
14.5%–15.1%，高值区间约 6.6%–6.9%。这也是采用树模型而非纯线性模型的主要
原因。

其他数值字段单变量 AUC 较弱，但可通过交互提供增量信息。`x0`–`x20` 增加均值、
标准差、范围、L1/L2 范数和正值数量等行级统计；类别字段同时保留原始形式，并
拆分 `month`、`t3`、`source`、`version`、`grades` 中明确的结构。

## 模型消融

| 模型 | 验证方式 | OOF AUC |
|---|---|---:|
| 正则化逻辑回归 | 分层 5 折 | 0.598 |
| ExtraTrees（仅数值） | 分层 5 折 | 0.637 |
| HistGradientBoosting（仅数值） | 分层 5 折 | 0.642 |
| XGBoost | 严格嵌套、重复分层 5 折 | 0.6430 / 0.6470 |
| CatBoost | 严格嵌套、重复分层 5 折 | 0.6488 / 0.6444 |
| 固定 50/50 集成 | 严格嵌套、重复分层 5 折 | **0.6484 / 0.6491** |

固定集成的均值为 **0.64873**，两次重复间标准差为 **0.00034**。早停只使用
外层训练折内部再切出的数据；选定迭代数后，模型在完整外层训练折重训，外层
验证标签不参与早停或模型选择。集成权重在训练前固定，不根据同一份 OOF 或
公开榜单搜索。

## 结论与风险

现有数据上的严格验证证据支持约 0.648 的泛化 AUC，不能诚实地声称一定达到
公开榜 0.759。公开榜样本量、榜单方差或其他选手是否利用不稳定模式均未知。
当前提交优先最小化泄漏和榜单过拟合风险；若允许提交反馈，反馈只能作为一次
独立外部检验，不能据此反复调整参数。
