# B6pro 状态

## 公开榜对齐（关键）

| 项 | 值 |
|---|---|
| 方案 | `max(gap, gap_bag, plus_v10)` ≡ B7 closest ≡ 本分支冻结 |
| 本地 nested | **0.702704955** |
| 你提交后的公开榜 | **0.707** |
| 目标 | 诚实本地 nested **≥ 0.71**（缺口 ≈0.0073） |

## Closest

仍为 **0.70270496**。后续大量异构/门控/专模/加权实验均未超过。

## 切片线索

对抗验证 train↔test AUC≈0.88：max3 在「最不像 test」切片可达 **~0.716**，在「最像 test」切片 ~0.698。正在用 test-like 加权臂冲击难切片。
