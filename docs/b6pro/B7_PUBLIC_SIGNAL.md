# B6pro ← B7 公开榜信号

## 事实对齐

| 项 | 值 |
|---|---|
| B7 closest 本地 nested | **0.702704955** |
| B6pro 冻结（同配方） | **0.702704955**（OOF 逐点一致） |
| 用户提交文件 | `submission_b7_closest_honest.csv`（分支 `cursor/b7-push-auc071-a5f5`） |
| **公开榜** | **0.707** |
| 公开 − 本地 | **≈ +0.0043** |

配方（相同）：

```text
elementwise_max(B6_gap_8seed, B6_gap_bag_8seed, V10_plus_h2_10)
```

结论：该方案**不是过拟合空壳**；公开侧略高于本地。下一步在**不作弊、不换测集标签**前提下，用异构臂 / 嵌套门控把本地 nested 往 **0.71** 推。

## 目标换算（仅启发式，不作宣称）

若公开−本地间隙大致稳定 ~0.004，则本地 0.71 可能对应公开 ~0.714；仍以**本地诚实 nested**为准。
