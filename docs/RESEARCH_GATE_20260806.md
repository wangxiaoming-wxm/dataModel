# 0.72 门禁续研记录

## 交接复核

- V1 OOF 直接由交接包 `models/oof_v1_3rd.npy` 重算：
  `0.6929595789847504`。
- V1 提交 SHA-256：
  `49ece5755addf909ef0fd36146d3a1341de0fc5abf4f74a941d87f97df6663b8`，
  与第三名相同，禁止提交。
- V4_quick SHA-256：
  `96ac8575370ecb69f7933267e4d08090989d1131d6bcf93322327b6198d8dbe5`。
- V4_quick OOF `0.6921064403419052` 只能从交接元数据核对；交接包未包含
  Q1/Deepseek OOF 数组，因此不能从底层预测逐位重算。该证据缺口不影响“不交”
  决策。
- sem_plus seed2026 OOF 重算为 `0.6826800643383284`。

## P0：sem_plus 止损

sem_plus 相对 V1 低 `0.01028`，固定 seed `73`、3,000 次配对 bootstrap 的
V1 优势 95% CI 为 `[0.00656, 0.01388]`，OOF Spearman 为 `0.96388`。劣势明确，不续跑其余
seed，也不进入 shuffled 门禁。

## P1：异构模型筛选

| 新臂 | 诚实筛选结果 | 与 V1 OOF Spearman | 固定等权融合 | 决策 |
|---|---:|---:|---:|---|
| EBM / GAM | 0.64922 | 0.87585 | 0.67725 | 淘汰 |
| RealMLP-TD | 前两折 0.59942 / 0.60052 | — | 未融合 | 提前淘汰 |
| TabM | 首折 0.57451 | — | 未融合 | 提前淘汰 |

EBM 与 V1 的等权 rank 融合相对 V1 下降 `0.01571`，配对 bootstrap 95% CI
`[-0.01947, -0.01196]`。Bootstrap 固定使用 seed `73`、3,000 次逐行有放回
配对抽样和 `[0.025, 0.975]` 分位数。RealMLP 和 TabM 的早期折结果远离可晋级区间，按惜枪
与算力止损原则中止；未选择性补报最好折。

## 门禁判定

- 无外部数据、伪标签、半监督、目标编码或 OOF 权重寻优。
- 新模型的预处理只在外层训练折拟合，外层验证标签不参与内部早停。
- 所有新臂均在性能筛选阶段失败，因此不启动多 seed 与完整 shuffled 训练。
- 没有候选稳定高于 V1 `0.693`，`SUBMIT_GATE_0.72` 为 **FAIL**。
- 异构筛选阶段本身未产出冲 0.72 的正式提交文件。

后续在用户明确要求准备 1–2 枪时，于 `final_candidates/` 生成风险最优保底/条件文件；
完整总结与交枪文件名见 `docs/WORK_SUMMARY_20260806.md`。门禁结论仍为 FAIL（预期约 0.70–0.71）。

确定性复算脚本：`scripts/rebuild_research_gate.py`。机器可读证据：
`artifacts/research_gate_20260806.json`。
